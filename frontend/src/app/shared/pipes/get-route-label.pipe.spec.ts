import { GetRouteLabelPipe } from './get-route-label.pipe';

describe('GetRouteLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new GetRouteLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
